# particles-js-demo — agent instructions

作業前に **`docs/project-sync.md`** を読むこと。

## 実装時の必須事項

- **`src/`・`public/`・デモカタログ構成**を変えたら、同じ変更で `npm run sync:project-docs` を実行する（`docs/project-sync.md` の自動生成ブロックを更新）。
- 挙動・環境変数の説明は §「実行とデプロイ」以降も必要に応じて手で追記する。
